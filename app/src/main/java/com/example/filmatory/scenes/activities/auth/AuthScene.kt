package com.example.filmatory.scenes.activities.auth

import com.example.filmatory.scenes.SuperScene

/**
 * AuthScene is the main scene for authentication, everything related to authentication will extend this class
 *
 */
open class AuthScene : SuperScene()