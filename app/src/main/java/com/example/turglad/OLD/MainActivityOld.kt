package com.example.turglad.OLD

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.turglad.OLD.systems.ArSystem
import com.example.turglad.R

class MainActivityOld : AppCompatActivity() {
/*
    lateinit var arSystem: ArSystem;

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        arSystem = ArSystem(this, this);
        setContentView(R.layout.activity_main)
    }

    override fun onResume() {
        super.onResume()
        //Check camera permission
        arSystem.checkCameraPermission();
        arSystem.verifyArCore();
        arSystem.openCamera();
    }

    override fun onRequestPermissionsResult(
        requestCode: Int,
        permissions: Array<out String>,
        grantResults: IntArray
    ) {
        super.onRequestPermissionsResult(requestCode, permissions, grantResults)
        //Check permission camera
        arSystem.hasCameraPermissionResult(this, this)
        finish();
    }

 */
}